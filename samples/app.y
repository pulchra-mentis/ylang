# `start`: `app` -- main function
# app is marking this function to be the entry point of the application
(
    (2 3 min) 
prt) 

# `max`: `int` -- returns the max value
# `a`: `int` -- right-most value
# `b`: `int` -- left-most value
(b a (b a mor) ter)